# Claude 接線

## 最小接線

在專案 `CLAUDE.md` 或既有專案規則加入一段相對路徑指向，不把完整協作檔案貼進規則檔：

```markdown
## AI 協作檔案

- 需要理解使用者的工作偏好與確認界線時，讀取 `AI協作系統/profile/我的AI協作檔案.md` 的相關段落。
- 不把單次對話要求自動回寫成永久偏好。
```

若已有治理或 context budget，保留硬規則在 `CLAUDE.md`，把可按需讀取的背景留在協作檔案。新增前先比較既有內容並預覽，不覆蓋。

## 驗證

1. 開新對話，要求 Claude 摘要協作界線。
2. 使用同一張工作協作卡完成真實小工作。
3. 檢查平台工具與連接是否真的可用；未測試一律標「需另接」。
